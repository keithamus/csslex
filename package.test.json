Since this change is a modification of a JSON file (likely a `package.json` file in a Node.js project), we can write a test to check if the "license" field exists and if it is set to "MIT". Here is a simple test using Node.js and the built-in `assert` module:

```javascript
const assert = require('assert');
const package = require('./package.json');

describe('package.json', function() {
  it('should have MIT license', function() {
    assert.equal(package.license, 'MIT');
  });
});
```

This test will fail if the "license" field is missing or if it is set to something other than "MIT".